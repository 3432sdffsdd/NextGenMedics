{
    "name": "nextgen-medics/lms-api",
    "description": "NextGen Medics LMS REST API",
    "type": "project",
    "require": {
        "php": ">=8.3"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": ["@php -r \"echo 'Optional: composer autoload available. bootstrap.php works without it.\\n';\""]
    }
}
